services:
  diario-aleto-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diario_aleto_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/Araguaina
      - PYTHONUNBUFFERED=1
